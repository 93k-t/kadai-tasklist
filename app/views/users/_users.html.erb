<% if users.any? %>
  <ul class="list-unstyled">
    <% users.each do |user| %>
      <li class="mb-3 d-flex">
        <img class="round me-2" src="<%= gravatar_url(user,{size: 64}) %>" alt="">
        <div>
          <div>
            <%= user.name %>
          </div>
          <div>
            
          </div>
        </div>
      </li>
    <% end %>
  </ul>
  <%== pagy_bootstrap_nav(@pagy) %>
<% end %>